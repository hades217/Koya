# Agent instructions for this Demo

These instructions apply to the entire `hybrid_cloud_customer_service` directory.

1. Before deploying, updating, validating, or troubleshooting this Demo, read
   `.agents/skills/agentkit-hybrid-cloud-demo/SKILL.md` completely and treat it
   as the execution contract.
2. Read `README.md` for environment preparation, then read the requested file
   under `docs/steps/` before acting.
3. If the current agent does not implement automatic Skill discovery, use the
   repository `SKILL.md` as ordinary project instructions. No Codex-specific
   installation is required.
4. Do not substitute another similarly named or general AgentKit workflow for
   this project Skill.
5. Never print, persist, or commit credentials. Stop for the human-only actions
   and evidence requirements defined by the project Skill.
