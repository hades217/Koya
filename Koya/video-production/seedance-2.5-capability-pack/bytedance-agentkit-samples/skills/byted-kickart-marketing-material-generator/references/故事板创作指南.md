# 故事板创作工具使用指南
## 目录
- [一、概述](#一概述)
- [二、前置依赖](#二前置依赖)
- [三、命令行参数说明](#三命令行参数说明)
- [四、返回结果说明](#四返回结果说明)
- [五、使用示例](#五使用示例)
- [六、Agent 执行流程（强制遵守）](#六 agent 执行流程强制遵守)

## 一、概述
本工具用于基于已有的创意分析结果自动调用 AIGC 能力生成结构化故事板，最终输出标准 JSON 格式的故事板创作结果文件。

## 二、前置依赖
1. 已配置火山鉴权的 AK/SK，可正常调用接口
2. 环境已安装 Python 3.12+
3. 依赖 Python 第三方库：`requests`, `jsonpath`, `pydantic`

## 三、命令行参数说明
| 参数名 | 是否必填 | 类型 | 说明 |
|--------|----------|------|------|
| `--input` | 是 | String | 创意分析结果 JSON 文件的本地路径（支持绝对路径/相对路径） |
| `--output` | 是 | String | 生成的故事板结果 JSON 文件的保存路径，需包含完整文件名（后缀为`.json`） |

## 四、返回结果说明
### 成功返回
工具最终返回 Result 对象：
```json
{"code": "0", "message": "<故事板创作结果保存的文件绝对路径>"}
```

## 五、使用示例
```bash
# 1. 确保输出目录存在
mkdir -p /tmp/kickart/output

# 2. 生成各环节唯一文件名
CREATIVE_FILE="/tmp/kickart/output/creative_1742525912345_123456.json"
STORYBOARD_FILE="/tmp/kickart/output/storyboard_$(date +%s%N | cut -b1-13)_$((RANDOM%900000+100000)).json"

# 示例 1：基于创意分析结果生成故事板
python storyboard.py --input "$CREATIVE_FILE" --output "$STORYBOARD_FILE"

# 成功输出示例
提交任务成功，任务 ID: 123456789
{"code": "0", "message": "/tmp/kickart/output/storyboard_1742525956789_987654.json"}
```

## 六、Agent 执行流程（强制遵守）
1. **前置强校验**：调用脚本前必须先校验三个条件，任意不满足直接终止流程：
   - 上一环节（创意分析）的用户「确认创意无误」文字回复记录存在且有效
   - 创意分析结果、用户确认的需求参数完整无误
   - 当前主会话已按 `当前会话定位与 session 获取规范.md` 校验通过，后续 Task ID、用户确认记录、任务参数只允许写入该主会话上下文
2. 调用 exec 工具启动 `python storyboard.py` 脚本，设置≥180000ms（3 分钟）的 yieldMs
3. 若脚本未立即返回结果，先回复用户：
```txt
正在为您生成故事板，任务执行时间可能较长，请您稍候~ 
⚠️ 如长时间未收到结果，您可以稍后主动询问当前创作进度或结果。
```
4. 若脚本因超时/异常退出，必须基于已校验通过的当前主会话上下文读取持久化 Task ID，再调用任务查询接口确认后端状态，禁止直接判定任务失败
5. **结果强制确认**：任务完成后必须向用户同步故事板完整内容（分镜、口播、时长分配），获得用户明确的「确认使用该故事板生成视频」文字回复后，必须将该确认记录持久化到已校验通过的当前主会话上下文，方可进入后续视频生成流程；无确认记录不得跳转至下一环节
6. 若用户拒绝当前故事板，引导用户调整创意参数后重新生成故事板，重复以上流程
7. **错误熔断机制**：任意环节校验不通过时，自动终止流程，引导用户回到对应环节补充信息/完成确认，不得跳过校验执行后续操作