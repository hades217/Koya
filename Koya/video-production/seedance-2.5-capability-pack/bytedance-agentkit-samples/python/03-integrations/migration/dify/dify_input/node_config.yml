# Optional Dify node runtime configuration.
# This file is user-provided input for `agentkit migrate dify create`.
# It is not generated by the CLI and must not be overwritten by `status`.

llm:
  default:
    provider: ark
    model: ""
    api_key_env: ""

external_services: {}
